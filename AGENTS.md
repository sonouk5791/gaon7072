# 프로젝트 작업 지침

## 설명 방식
- 사용자는 코딩 초보자다. 작업 결과를 쉬운 한국어로 설명한다.
- 사용자가 분석만 요청하거나 수정을 금지한 경우 해당 범위를 지킨다.

## Markdown 작업 기록 자동 갱신
- 이 프로젝트에서 작업을 마칠 때 사용자가 매번 요청하지 않아도 `WORK_LOG_YYYY-MM-DD.md`에 작업 내용을 기록한다. 날짜는 한국 시간 기준 작업일을 사용한다.
- 해당 날짜의 파일이 있으면 기존 내용을 보존하며 추가하고, 없으면 새로 만든다. 기존 `WORK_SUMMARY.md`는 임의로 덮어쓰지 않는다.
- 요청 내용, 실제 변경한 파일과 변경 전후 요약, 수행한 확인 결과, 미해결 사항을 기록한다.
- 코드 분석으로 발견한 문제와 실제 실행으로 재현한 문제를 구분한다. 실행하지 않은 검사나 배포를 완료했다고 기록하지 않는다.
- 같은 날 추가 수정이 있으면 최종 상태 설명도 최신 내용에 맞추되 이전 변경 이력은 보존한다.
- 비밀번호, 인증키, 실제 상담 신청자의 개인정보는 기록하지 않는다.
- 사용자가 파일 변경을 명시적으로 금지한 작업에서는 기록 파일도 수정하지 않는다.
- 이 규칙은 이 지침을 읽고 수행하는 에이전트 작업에 적용된다. 외부 편집기의 변경을 감시하는 백그라운드 프로그램은 아니다.
